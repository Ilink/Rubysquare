var type_checker = function(provided, should_be, typecheck){
    /*
    for each property {
        search for provided.prop in should_be.prop {
            if ( provided.prop is not in should_be.prop ) throw 'Required parameter not found'
            else if ( typecheck ) {
                typeof provided.prop != typeof should_be[prop] throw 'wrong type for parameter'
            |
     }
     */
}